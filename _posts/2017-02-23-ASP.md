---
layout: post
title: ASP
---
* `gringo`  
  so-called grounder translating
* `clasp`  
  so-called solver
* `clingo`  
  結合gringo和clasp
<!--more-->

`:-`  if  
`,`   and  
`%`   comment  
`.`   end of sentence  
`_`   don't care  '
`-`   邏輯上否定
not   

man(m(1..nMen))  % m(1) m(2)
為什麼不直接用 man(1),man(2)...

domain prdicate

#count 
1 {} 1  等價於 1 <= #count{} <= 1

```
{p; q}.

4個model
p q 都錯
p 對 q 錯
q 錯 q 對
p q 都對
```

:- statement.
左邊沒寫預設是false,代表右邊必須是false,才能consist
才能得到 false -> false
:- mp(m(M), R1, w(W)), mp(m(M), R2, w(W)), R1 != R2. (!= 習慣用 <)
:- mp(m(M), R1, w(W)), mp(m(M), R2, w(W)), not R1 == R2.
not not R1 = R2:- mp(m(M), R1, w(W)), mp(m(M), R2, w(W)).
not not 只有驗證的功能,沒有生成的功能

要引入別的檔案
#include "xxx.lp"
變數分safe / unsafe
所有的變數都要positive(必須知道資料來源)

#count{W: match(m(M),w(W))}
: 在集合中表示| (例如 {W | 條件}) 

條件敘述使用`:` 
condition literal
p : q, r : r1, r2
有兩種情況
1. p : q , r.
2. p : q.

single(m(M)) :- man(m(M)), #false : woman(w(W), match(m(M),w(W)))

可以考慮改善clingo界面
使用clingo分析最佳語意？

### reference
* [Potassco](https://potassco.org/)
* [Running clingo](https://potassco.org/clingo/run/)
